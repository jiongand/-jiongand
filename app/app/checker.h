#ifndef CHECKER
#define CHECKER
#include<vector>
#include<fstream>
#include<iostream>
#include<string>
using namespace std;
class checker
{
	vector<int> c;vector <int>w;
public:
	void compare(const char*e,const char*a,const char*p);
};
#endif // !CHECKER



